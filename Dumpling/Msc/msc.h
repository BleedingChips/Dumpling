#pragma once
#include <string>
#include <map>
#include <vector>
#include <variant>
#include "../../Potato/parser.h"
namespace Dumpling
{
	/*
	struct MaterialShaderCode
	{

		struct Define {
			std::vector<std::tuple<std::wstring, std::wstring>> RefDefine;
			std::wstring Code;
		};

		struct Section
		{
			std::map<std::wstring, Define> Defines;
			std::wstring Name;
			std::vector<std::array<std::wstring, 2>> InputProperty;
			std::vector<std::array<std::wstring, 2>> OutputProperty;
		};

		struct Material {
			std::wstring Type;
			std::vector<Section> Sections;
		};

		std::map<std::wstring, Define> Defines;
		std::map<std::wstring, std::variant<float, int64_t, std::wstring>> Property;
		std::vector<>
	};
	*/
}